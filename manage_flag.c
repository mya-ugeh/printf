#include "main.h"
/**
 * check_flags - checks flags
 * @list - gets vardiaac list
 *
 * Return: returns flags
 */
int check_flags(va_list args)
{
/**	will add some code here **/
	char c = va_arg(args, int);
	return (c);
}

/**
 * print_flags - prints flags
 * @list - vardic list
 *
 * Return - returns blah blah
 */ 
int print_flags(va_list args)
{
	/**
	 * add some code here*/
	char c = va_arg(args, int);
	return (c);
}
